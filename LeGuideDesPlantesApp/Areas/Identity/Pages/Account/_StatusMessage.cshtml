@model string

@if (!String.IsNullOrEmpty(Model))
{

    var statusMessageClass = Model.StartsWith("Error") ? "danger" : "success";
    <div class="justify-content-center">
        <h4 class="alert alert-@statusMessageClass alert-dismissible" role="alert"> @Model </h4>
    </div>

}
